git 사용법
1. git init : git 초기화
2. git add .: 변경된 모든것을 추가
3. git commit -m "first message"
4.  git branch -M main :git이름을 main으로 설정
5. git remote add origin https://github.com/sunho45/c-.git : 원격저장소로 사용할 주소 여녁ㄹ
6. git push -u origin main :처음 설정할 땐 -u붙이기 , 설정하고 나선 -u 안붙여도 됨